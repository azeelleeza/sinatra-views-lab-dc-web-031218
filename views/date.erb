<!DOCTYPE html>
<html>
    <head>
        <title>Date</title>
    </head>
    <body>
        <h1>Today</h1>
        <% today = Time.now %>
        <!-- <p><%= "The date is " + today.strftime("%A, %B %-d, %Y")%></p> -->
        <p><%= "The date is " + today.strftime("%A, %B %d, %Y")%></p>
    </body>
</html>
